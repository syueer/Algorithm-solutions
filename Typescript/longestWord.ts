let x = 'fun&!!time';

const longestWord = (sen: string): string => {
  let array = sen.split('');
  for (let i = 0; i < array.length; i++) {}

  console.log(array);
  return '';
};

console.log(longestWord(x));
